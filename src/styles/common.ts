import { StyleSheet } from 'react-native'

export const commonStyle = StyleSheet.create({
  p10: {
    padding: 10,
  },
  absolute: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
})
